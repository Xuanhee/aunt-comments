import { defineComponent } from 'vue'
import './{{name}}.css'

export default defineComponent({
  name: '{{name}}',
  setup(props, { emit }) {
    return () => {
      return (
        <a-card>
        </a-card>
      )
    }
  },
})